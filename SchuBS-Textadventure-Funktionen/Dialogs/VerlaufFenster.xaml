<Window x:Class="SchuBS_Textadventure.Dialogs.VerlaufFenster"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SchuBS_Textadventure.Dialogs"
        xmlns:Verlauf="clr-namespace:SchuBS_Textadventure.Objects.Verlauf"
        mc:Ignorable="d"
        Title="Verlauf" Height="450" Width="400"
        Background="Black">
    <Window.Resources>
        <Style TargetType="TextBlock" >
            <Setter Property="Foreground" Value="White" />
            <Setter Property="TextWrapping" Value="Wrap" />
            <Setter Property="Margin" Value="3,0" />
            <Setter Property="FontSize" Value="14" />
        </Style>
    </Window.Resources>

    <ScrollViewer x:Name="ScrollViewerVerlauf" Loaded="ScrollViewer_Loaded">
        <StackPanel x:Name="StackPanelVerlaufText" />
    </ScrollViewer>
   
</Window>
